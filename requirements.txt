# Vercel deployment manifest (self-hosters: use `uv sync` / pip install .[...]).
# Single requirement line: Vercel's Python builder skipped the separate
# `psycopg[binary]` line, crashing the deployed function with
# ModuleNotFoundError (observed live 2026-07-20) — the postgres driver must
# come in via the package extra instead.
.[postgres]
